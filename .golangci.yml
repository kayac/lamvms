version: "2"

run:
  tests: false

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  settings:
    errcheck:
      check-type-assertions: true

formatters:
  enable:
    - gofmt

issues:
  exclude-dirs:
    - testdata
  exclude-generated: strict
